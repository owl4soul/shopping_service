# Adjusting the root URI for rest api
spring.data.rest.base-path=/api
spring.security.filter.order=3
spring.security.user.name=admin
spring.security.user.password=password


## default connection pool
spring.datasource.hikari.connectionTimeout=20000
spring.datasource.hikari.maximumPoolSize=5

## PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/alians
spring.datasource.username=lera2
spring.datasource.password=lera2

#drop n create table again, good for testing, comment this in production
spring.jpa.hibernate.ddl-auto=update

#to avoid error on start
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true